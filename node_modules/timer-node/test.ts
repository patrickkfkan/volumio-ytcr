import { Timer, Time } from './index';

const fn = (a) => {
    let sum = 0;
    for (let i = 0; i < 10000000; i += 1) {
      sum += a * i;
    }
    return sum;
  }
  const t: number = Timer.benchmark(fn.bind(fn, 5)).time();
  
  console.log(t);
